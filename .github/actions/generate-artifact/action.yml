name: Generate artifact file
description: Creates artifact.txt with a timestamped message

runs:
  using: composite
  steps:
    - name: Write artifact file
      shell: bash
      run: |
        echo "This will be stored as an artifact!" > artifact.txt
        echo "Generated by local composite action on $(date)" >> artifact.txt
